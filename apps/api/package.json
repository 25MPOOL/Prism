{
	"name": "api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify src/index.ts",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"start": "wrangler dev src/index.ts",
		"db:generate": "drizzle-kit generate",
		"db:migrate:local": "wrangler d1 migrations apply DUMMY_D1_DATABASE_NAME --local",
		"db:migrate:prod": "wrangler d1 migrations apply DUMMY_D1_DATABASE_NAME",
		"db:studio": "drizzle-kit studio"
	},
	"dependencies": {
		"drizzle-orm": "^0.44.4",
		"hono": "^4.8.10"
	},
	"devDependencies": {
		"drizzle-kit": "^0.31.4",
		"wrangler": "^4.4.0"
	}
}
